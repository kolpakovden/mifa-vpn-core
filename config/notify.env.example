# May reuse the admin bot token or use a separate bot.
BOT_TOKEN=
NOTIFY_CHAT_ID=
NOTIFY_WINDOW_MINUTES=5
# Optional geo lookup using ip-api.com over HTTP.
NOTIFY_GEO=0
XRAY_ACCESS_LOG=/var/log/xray/access.log
